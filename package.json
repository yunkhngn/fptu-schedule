{
  "name": "fptu-schedule",
  "version": "3.6.6",
  "private": true,
  "description": "Tests for the FPTU Schedule Chrome extension. The extension itself ships as plain files; npm is only used to run these.",
  "scripts": {
    "test": "node --test tests/*.test.js"
  },
  "devDependencies": {
    "jsdom": "^26.0.0"
  }
}
